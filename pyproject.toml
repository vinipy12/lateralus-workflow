[project]
name = "lateralus-workflow"
version = "0.1.0"
description = "Discuss-first planning and execution workflow for Codex"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
