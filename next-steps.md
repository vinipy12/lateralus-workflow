# Next Steps

## Remaining Kernel Follow-Up

- Revisit packaging and distribution once the current `$workflow` and `$ship` kernel surface stays stable.
- Add telemetry polish if maintainers need richer summaries beyond the current repo-local counters and durations.
- Add bootstrap refinements only where real usage exposes gaps beyond the current `bootstrap-start` router flow.

## Not Remaining

- Post-code UAT, gap-closure, and replan states are now part of the execution kernel.
- Local telemetry now persists under `.codex/workflow/metrics/`.
- Greenfield bootstrap planning now ships on the existing router surface.
